'use strict';

/**
 * In-Memory Service
 * 
 * Used to store information in-memory
 * We will be using Redis for now
 * 
 * The service should support all CRUD operations on a data
 * We may not require the expiry as an information attached to a data
 */

class IMS {

    constructor() {

    }

    save() {

    }

    get() {

    }

    delete() {

    }
}

let ims = new IMS();

module.exports  = ims;